{
  "name": "construction-client-portal",
  "version": "1.0.0",
  "description": "Construction Client Design Portal - Full Stack Application",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm --prefix backend ci && npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "start:backend": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
